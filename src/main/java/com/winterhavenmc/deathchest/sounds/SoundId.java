package com.winterhavenmc.deathchest.sounds;


/**
 * An enum that whose values represent the in game sounds played by the plugin corresponding to
 * user configured settings in the sounds.yml file.
 */
public enum SoundId {

	CHEST_BREAK,
	CHEST_DENIED_ACCESS,
	COMMAND_FAIL,
	INVENTORY_ADD_ITEM,
	COMMAND_INVALID,

}
